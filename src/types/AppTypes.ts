export type AuthData = {
  accessToken: string;
  refreshToken: string;
  userName: string;
};

export type RegistrationData = {
  userName: string;
  password: string;
  email: string;
  firstName: string;
  lastName: string;
};
